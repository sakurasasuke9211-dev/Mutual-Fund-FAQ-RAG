# citation/

Resolves exactly one allowlisted Groww scheme URL per answer.

| Module | Role |
|--------|------|
| `resolver.py` | `CitationResolver` — top chunk metadata + corpus allowlist |

Allowlist source: `config/corpus_manifest.yaml` via `ingestion/manifest.py`.
